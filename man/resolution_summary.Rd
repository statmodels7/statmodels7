% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_context.R
\name{resolution_summary}
\alias{resolution_summary}
\title{The Resolution the Search Is Given, Out of the Readings Taken So Far}
\usage{
resolution_summary(x)
}
\arguments{
\item{x}{Every usable reading so far.}
}
\value{
A single positive number, or \code{NA_real_} where there is none.
}
\description{
The \strong{largest} of the usable readings \code{\link[=criterion_resolution]{criterion_resolution()}} has
returned during this search.
}
\details{
A reading is taken at every usable evaluation, and each one asks how far
the criterion moves when the mode is displaced by the error \strong{that}
evaluation's inner fit happened to stop at. The displacement varies across
evaluations by far more than the quantity it stands for does, so which
reading is kept decides the answer.

\strong{The readings are already filtered to acceptable modes}, which is what
makes the largest of them the right one to keep: \code{\link[=criterion_resolution]{criterion_resolution()}}
returns \code{NA} where the mode error exceeds \code{\link[=mode_error_limit]{mode_error_limit()}}, so every
reading that reaches here comes from a mode the layer would accept, and
what a resolution has to bound is how far the criterion \strong{can} move
between two such modes -- not how little it happened to move at the
luckiest evaluation.

\strong{What the smallest cost}, measured directly. At one hyperparameter
reached from six different warm starts on a \code{pig1} smooth, the criterion's
own spread is \code{2.3e-06} under \code{iwls(hessian = "expected")} and
\code{4.7e-07} under \code{"observed"} -- a factor of 5 between the two branches.
The readings taken during those searches run \code{1.8e-07} to \code{3.5e-06} and
\code{1.1e-10} to \code{1.2e-07}, so the largest of them is within a factor of 4 of
the spread in both (1.5 times high and 3.9 times low), where the smallest
is 13 times low in the first and \strong{4300} times low in the second. The
inner score at which a Newton step stops varies by three orders across
evaluations, and a minimum over such a sequence reads the luckiest one.

A resolution far below the truth does not stop a search early, it stops it
from ever returning: the line search goes on backtracking for improvements
the criterion cannot resolve, and the run ends by exhausting
\code{\link[=outer_backtracks]{outer_backtracks()}} instead of on a rule. Measured over 60 fits of ten
shapes at three seeds each, the largest reading against the smallest:
\strong{368 criterion evaluations against 577}, the search's own flag met in 58
against 43, no flag lost anywhere, and \code{statmod_certificate()} returning
\strong{the same verdict on every one of the 60} -- the resolution decides what
a search costs and not where it arrives. Under \code{hessian = "observed"}
alone it is 173 evaluations and 30 flags of 30 against 334 and 18. What is
given up is at most \code{1.1e-05} of criterion on a criterion of order
\eqn{10^2} to \eqn{10^3}, with a median of \code{8.7e-07} over the fits where
anything is given up at all.
}
\seealso{
\code{\link[=criterion_resolution]{criterion_resolution()}}, \code{\link[=outer_fit]{outer_fit()}}
}
\keyword{internal}
