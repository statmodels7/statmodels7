% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer.R
\name{index_members}
\alias{index_members}
\title{The Hyperparameters Behind Each Row of the Outer Index}
\usage{
index_members(idx)
}
\arguments{
\item{idx}{The index, as \code{\link[=outer_hyper_index]{outer_hyper_index()}} returns it.}
}
\value{
A data frame with columns \code{row}, \code{parameter}, \code{term} and \code{name}.
}
\description{
The member table: one line per estimated hyperparameter, with the row of
the index that stands for it.
}
\details{
Where nothing is shared it is the index itself with a \code{row} column counting
up, so a loop over members is the loop over rows that was there before
sharing existed. Where a label ties several, they carry the same \code{row},
and a quantity accumulated over members lands on the group's coordinate.
}
\keyword{internal}
