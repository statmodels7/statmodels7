% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path.R
\name{cv_curve}
\alias{cv_curve}
\title{The Held-Out Deviance of Every Point of a Path}
\usage{
cv_curve(
  spec,
  data,
  weights,
  offsets,
  inner_optimizer,
  hypers,
  folds,
  run = NULL
)
}
\arguments{
\item{spec}{A \code{\link[=StatmodSpec]{StatmodSpec()}}.}

\item{data}{The data the fit was called on.}

\item{weights, offsets}{As \code{\link[=statmod]{statmod()}} received them.}

\item{inner_optimizer}{The inner method.}

\item{hypers}{A list of hyperparameter settings, one per path point.}

\item{folds}{A fold number per observation.}

\item{run}{Which combination of the outer axes each point belongs to. The
warm start begins again at the head of each, the kink jumping back up
there. \code{NULL} treats the whole list as one run.}
}
\value{
A list with \code{cvm}, \code{cvse} and \code{n_fail}.
}
\description{
Refits the model on each training fold along the whole path and scores it on
the fold left out, returning the mean deviance per observation and its
standard error across folds.
}
\details{
The path is run fold by fold rather than point by point so that each fit
starts from the previous point's coefficients, which is what makes a path
cheaper than its length suggests. Each training fit rebuilds the design on
its own rows: a term is re-evaluated in the data it is fitted to, so a basis
or a set of contrasts is not carried over from rows the fit did not see.
}
\seealso{
\code{\link[=cv]{cv()}}
}
\keyword{internal}
