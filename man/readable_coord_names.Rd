% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{readable_coord_names}
\alias{readable_coord_names}
\title{A Coordinate's Name in Place of Its Number}
\usage{
readable_coord_names(nm, labels)
}
\arguments{
\item{nm}{The names as the family gives them.}

\item{labels}{One label per coordinate, in the matrix's own order.}
}
\value{
A character vector as long as \code{nm}.
}
\description{
Rewrites the names a multivariate family gives the quantities of a matrix
parameter -- \code{sd_v1}, \code{cor_v1_v2} -- into the same quantities said of the
coordinates this model is written in: \code{sd[mu:(Intercept)]},
\code{cor[mu:(Intercept), sigma:(Intercept)]}.
}
\details{
The family names its coordinates by position because that is all it has,
and every one of its readings -- a standard deviation, a correlation, a
partial correlation, a conditional scale -- is named \code{prefix_vi} or
\code{prefix_vi_vj}. The rewrite is on that shape alone: a name it does not
match, or one whose index no label answers for, is left exactly as it is,
so a family declaring a reading of another kind keeps its own name rather
than being renamed into a wrong one.
}
\seealso{
\code{\link[=class_coords]{class_coords()}} and \code{\link[=term_coord_labels]{term_coord_labels()}} for the labels,
\code{\link[distributions7:mv_derived]{distributions7::mv_derived()}} for the names being rewritten.
}
\keyword{internal}
