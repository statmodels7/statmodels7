% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective.R
\name{information_subset}
\alias{information_subset}
\title{The Information Over Some of the Coefficients}
\usage{
information_subset(spec, design, ev, expected, approx, H, index)
}
\arguments{
\item{spec}{A \code{\link[=StatmodSpec]{StatmodSpec()}}.}

\item{design}{The design, already refreshed at the coefficients.}

\item{ev}{What \code{\link[=statmod_eta]{statmod_eta()}} returns at the coefficients.}

\item{expected, approx}{As in \code{\link[=statmod_information_at]{statmod_information_at()}}.}

\item{H}{The second-derivative components, or \code{NULL} to ask for them.}

\item{index}{Integer positions in the stacked coefficients.}
}
\value{
A symmetric matrix over \code{index}, in that order: a \pkg{Matrix}
object when any equation's design is sparse, a base matrix otherwise.
}
\description{
The rows and columns of \code{\link[=statmod_information_at]{statmod_information_at()}} at the given positions,
assembled from those columns of the designs alone.
}
\details{
Block \eqn{(a, b)} of the result is \eqn{X_{a,J_a}'\,\mathrm{diag}(w\,
h_{ab})\,X_{b,J_b}} over the columns \eqn{J_a} and \eqn{J_b} the
positions select in each equation, which is exactly the corresponding
submatrix of the whole information and costs a cross product over those
columns instead of over all of them. A mixture over regimes sums the
components' blocks as the whole matrix does.
}
\seealso{
\code{\link[=statmod_information_at]{statmod_information_at()}}
}
\keyword{internal}
