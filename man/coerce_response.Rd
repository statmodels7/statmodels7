% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{coerce_response}
\alias{coerce_response}
\title{Carry a Categorical Response onto the Numeric Scale}
\usage{
coerce_response(response, levels = NULL)
}
\arguments{
\item{response}{The evaluated left-hand side.}

\item{levels}{The mapping to reuse, from a previous call's attribute, or
\code{NULL} to read it off this response.}
}
\value{
\code{response} unchanged, or a numeric vector of 0 and 1 carrying the
attribute \code{"response_levels"}.
}
\description{
Returns the response as a number, converting a two-level \code{factor},
\code{character} or \code{logical} to 0 and 1 and leaving anything else alone.
}
\details{
A binary outcome recorded as a factor is how these data are ordinarily
written, and \code{glm()} has always accepted one: its binomial family reads the
first level as the failure. Without this the response reached
\code{stats::dbinom()} untouched and the run died on \emph{"Non-numeric argument to
mathematical function"}, which names neither the variable nor the cause.

The first level is the failure, which is \code{glm()}'s rule, so a model moved
from \code{glm()} keeps its signs. The conversion is made for EVERY family and
not only for the binary ones, which is where this departs from base R --
and it departs in the direction of working: \code{glm(f ~ x, gaussian)} on a
factor \code{f} does not refuse cleanly, it signals \emph{"NA/NaN/Inf in 'y'"} after
three warnings from \code{Ops.factor}. A two-level factor has one numeric
reading and this is it; on a gaussian family the result is the linear
probability model, which is what \code{as.numeric(f == "b")} would have given.

The levels used are returned as the attribute \code{"response_levels"}. That is
what lets \code{\link[=statmod_respec]{statmod_respec()}} reproduce the SAME mapping on other rows: a
factor carries its levels through a subset, but a character vector does
not, so a cross-validation fold holding only one of the two values would
otherwise be coded the other way round and the deviance would come back
wrong without a word.
}
\seealso{
\code{\link[=statmod_spec]{statmod_spec()}}, \code{\link[=statmod_respec]{statmod_respec()}}
}
\keyword{internal}
