% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/order.R
\name{order_available}
\alias{order_available}
\title{Whether a Family Carries Enough Derivatives}
\usage{
order_available(distrib, need)
}
\arguments{
\item{distrib}{A distributions7 family.}

\item{need}{The order asked for; see the table in \code{R/order.R}.}
}
\value{
A single logical. \code{NA} orders count as unavailable: a wrapper of a
kinked family records the kink without declaring its order, and an
unestablished order is not evidence of smoothness.
}
\description{
\code{TRUE} when the log-density has at least \code{need} continuous derivatives in
every parameter the model carries an equation for. Used as a gate by the
consumers that can fall back on something else, and through
\code{\link[=order_shortfall]{order_shortfall()}} as a refusal by the ones that cannot.
}
\examples{
statmodels7:::order_available(distributions7::gaussian1_distrib(), 4L)
statmodels7:::order_available(distributions7::laplace_distrib(), 2L)

}
\seealso{
\code{\link[=order_shortfall]{order_shortfall()}} for the message, and
\code{\link[distributions7:params_order]{distributions7::params_order()}} for the quantity.
}
\keyword{internal}
