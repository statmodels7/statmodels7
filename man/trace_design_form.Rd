% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_gradient.R
\name{trace_design_form}
\alias{trace_design_form}
\title{The Trace Against a Contraction, Without Forming It}
\usage{
trace_design_form(spec, G, deriv, params, npar, tv, tu = NULL)
}
\arguments{
\item{spec}{A \code{\link{StatmodSpec}}.}

\item{G}{The per-observation diagonals, from \code{\link{block_leverage}}.}

\item{deriv}{The third or fourth derivatives on the link scale.}

\item{params, npar}{The parameter names and block sizes.}

\item{tv}{The direction the derivative is contracted in.}

\item{tu}{A second direction, for a fourth derivative; \code{NULL} for a
third.}
}
\value{
A single number.
}
\description{
\eqn{\mathrm{tr}(M\,T)} where \eqn{T} is the third derivative of the
penalized objective contracted once, or the fourth contracted twice.
}
\details{
\code{\link{contract3}} and \code{\link{contract4}} assemble
\eqn{-X_a'\,\mathrm{diag}(\omega_i w_i)\,X_b} block by block, and where the
result is only ever traced against \eqn{M} the assembly is waste: the trace
of that block against \eqn{M_{ab}} is \eqn{-\sum_i \omega_i w_i G_{ab,i}}.
The block and its transpose both contribute, so an off-diagonal pair counts
twice.

The weights \eqn{w_i} are built exactly as those two functions build them,
which is what keeps the two routes the same arithmetic in the same order
where it matters.
}
\seealso{
\code{\link{contract3}}, \code{\link{contract4}}
}
\keyword{internal}
