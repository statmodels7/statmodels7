% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_context.R
\name{ctx_penalized}
\alias{ctx_penalized}
\title{The Penalized Matrix and Its Inverse}
\usage{
ctx_penalized(ctx, spec, design, coef, hyper)
}
\arguments{
\item{ctx}{A context, or \code{NULL}.}

\item{spec, design, coef, hyper}{The fallback arguments.}
}
\value{
A list with \code{K} and \code{inv}, or \code{NULL}.
}
\description{
\eqn{K = H + S} with the OBSERVED information, its Cholesky factor and its
inverse, which the gradient and the Hessian both read.
}
\details{
The inverse of a sparse matrix is dense, so densifying costs nothing
sparsity could have kept. \code{NULL} is returned where the factorization
fails, which is the answer both callers already gave there.
}
\keyword{internal}
