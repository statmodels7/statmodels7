% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_hessian.R
\name{statmod_structural_hess}
\alias{statmod_structural_hess}
\title{The Exact Outer Hessian of a Model Carrying a Structural Term}
\usage{
statmod_structural_hess(spec, design, coef, hyper, method, idx, basis = NULL)
}
\arguments{
\item{spec}{A \code{\link[=StatmodSpec]{StatmodSpec()}}.}

\item{design}{The design.}

\item{coef}{The coefficients at the penalized mode.}

\item{hyper}{The hyperparameters.}

\item{method}{An \code{\link[=OuterMethod]{OuterMethod()}}.}

\item{idx}{The outer index.}

\item{basis}{The integrated subspace, or \code{NULL}.}
}
\value{
A square matrix on the free scale, one row per row of \code{idx},
or \code{NULL} where the joint matrix could not be formed.
}
\description{
\code{\link[=statmod_marginal_hess]{statmod_marginal_hess()}} over the joint vector of coefficients and
a filter's own parameters, which is what the determinant spans there.
}
\section{Why it needs a fourth order}{
Each order of differentiating the predictor through the recursion pulls in
one more order of the response's family, the score the recursion is driven
by being read at the predictor it produces. The gradient reads
\eqn{\partial^3 e/\partial u^3} in one direction through
\code{\link[modelterms7:term_third]{modelterms7::term_third()}}; the criterion's own second derivative
reads \eqn{\partial^4 e/\partial u^4} in two, through
\code{\link[modelterms7:term_fourth]{modelterms7::term_fourth()}}, and the family's FIFTH derivative with
it.
}

\section{The shape is \code{\link[=statmod_marginal_hess]{statmod_marginal_hess()}}'s}{
With \eqn{u} the joint vector, \eqn{K} the penalized information over it
and \eqn{M} the matrix the trace is taken against,

\deqn{\frac{\partial^2 V}{\partial t_m\partial t_l}
  = -\rho_{ml} + \hat b_m^\top K \hat b_l
  + \tfrac{1}{2}\mathrm{tr}(MK_lMK_m)
  - \tfrac{1}{2}\mathrm{tr}\Big(M\frac{\partial K_m}{\partial t_l}\Big),}

with \eqn{\hat b_m = -K^{-1}c_m} the mode's movement, \eqn{K_m = S_m +
\partial K/\partial u[\hat b_m]} and the last trace carrying the penalty's
second derivative, the twice-contracted second derivative of \eqn{K} and
the once-contracted one at the mode's second movement. Every piece is the
one the coefficient-space assembly uses, read on the joint vector.
}

\section{What it does not carry}{
A block that MOVES with its coefficients -- \code{nl()}, \code{seg()} --
beside the filter contributes nothing here, exactly as it contributes
nothing to \code{\link[=statmod_structural_grad]{statmod_structural_grad()}}: that correction is written
in the coefficient-space assembly and has no joint twin. Such a model is
admitted at both orders and the approximation is the gradient's own.
}

\section{Cost}{
One \code{\link[modelterms7:term_fourth]{modelterms7::term_fourth()}} and three lower recursions per PAIR
of hyperparameters, against the stencil's four refits per hyperparameter.
}

\seealso{
\code{\link[=statmod_structural_grad]{statmod_structural_grad()}}, \code{\link[=statmod_marginal_hess]{statmod_marginal_hess()}},
\code{\link[=statmod_hess_stencil]{statmod_hess_stencil()}} for the route it replaces.
}
\keyword{internal}
