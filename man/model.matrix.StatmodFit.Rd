% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{model.matrix.StatmodFit}
\alias{model.matrix.StatmodFit}
\title{The Design of One Equation}
\usage{
\method{model.matrix}{StatmodFit}(object, what = NULL, ...)
}
\arguments{
\item{object}{A \code{\link[=StatmodFit]{StatmodFit()}}.}

\item{what}{Which distribution parameter, or \code{NULL} for the first.}

\item{...}{Unused.}
}
\value{
An \verb{n x p} matrix, \code{p} being the width of that equation's design,
carrying the coefficient names as its column names. A \pkg{Matrix}
object where the equation's own blocks are sparse, a base matrix
otherwise. Zero columns for an equation carrying only a structural term.
}
\description{
The block of columns a distribution parameter's equation was fitted with.
}
\details{
A fit has one design per parameter, so which one is wanted is an argument.
\code{NULL} gives the first, as \code{\link[=fitted.StatmodFit]{fitted.StatmodFit()}} does. A term whose block moves with its
coefficients is returned at the fitted ones, which is the block the fit
ended on.

A structural term contributes no columns at all, so an equation carrying
one alone gives a matrix of no columns; what such a term contributes is a
recursion, reported by \code{\link[=predict.StatmodFit]{predict.StatmodFit()}}.
}
\examples{
set.seed(1)
dd <- data.frame(x = runif(40))
dd$y <- 1 + dd$x + rnorm(40, sd = 0.3)
fit <- statmod(y ~ x | sigma ~ x, distributions7::gaussian1_distrib(), dd)
dim(model.matrix(fit))
colnames(model.matrix(fit, "sigma"))
}
\seealso{
\code{\link[=statmod_design]{statmod_design()}} for every equation at once,
\code{\link[=coef.StatmodFit]{coef.StatmodFit()}} for the coefficients these columns multiply
}
\keyword{internal}
