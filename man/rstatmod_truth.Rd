% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rstatmod.R
\name{rstatmod_truth}
\alias{rstatmod_truth}
\title{Draw the Whole Truth of a Simulation}
\usage{
rstatmod_truth(spec, design, par, sd)
}
\arguments{
\item{spec}{The specification, built against a placeholder response.}

\item{design}{Its design.}

\item{par}{A named list, or \code{NULL}. See \code{\link[=rstatmod]{rstatmod()}} for what a key may be.}

\item{sd}{The width of the draws.}
}
\value{
A list with \code{coef}, one numeric vector per distribution parameter;
\code{hyper}, a data frame of the hyperparameters drawn or held; and \code{psi},
the structural term's own parameters or \code{NULL}.
}
\description{
Every coefficient, every hyperparameter and, where the formula carries a
structural term, that term's own parameters: drawn once, then overwritten
by whatever \code{par} names.
}
\details{
The rule the whole function is written from is that \strong{whoever knows what a
quantity means draws it}. A coefficient of a design column has no other
owner and is drawn from a normal of width \code{sd}. A coordinate some penalty
covers is drawn from that penalty read as a prior, so a Gaussian random
effect gives Gaussian effects and a lasso gives Laplace ones; the prior's
own scale is drawn too, on the chart its penalty carries. A structural
term's parameters are drawn by the term, which knows their charts.

The truth is drawn once whatever \code{n_sim} is, so the replicates differ in
what is random and not in what is being estimated.

A hyperparameter the term holds is used as given rather than drawn:
\code{s(x, bspline_smooth(), hyper = c(lambda = 2))} says what the smoothing is and the simulation says it
too.
}
\seealso{
\code{\link[=rstatmod]{rstatmod()}}, \code{\link[penalties7:penalty_draw]{penalties7::penalty_draw()}},
\code{\link[modelterms7:term_draw]{modelterms7::term_draw()}}
}
\keyword{internal}
