% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statmod.R
\name{statmod_penalized_at}
\alias{statmod_penalized_at}
\title{The Penalized Information at a Point}
\usage{
statmod_penalized_at(
  spec,
  coef,
  design,
  hyper,
  expected = TRUE,
  approx = "opg"
)
}
\arguments{
\item{spec}{The specification.}

\item{coef}{A named list of coefficients, one vector per distribution
parameter.}

\item{design}{The design.}

\item{hyper}{The hyperparameters.}

\item{expected}{Whether the expected information is used.}

\item{approx}{How the expected information is approximated.}
}
\value{
A dense symmetric matrix over the coefficients of every equation.
}
\description{
\eqn{H + S}, dense, with the penalty's non-finite entries zeroed.
}
\details{
The matrix a penalized fit's curvature is read from: the model's
information from \code{\link[=statmod_information_at]{statmod_information_at()}} and the penalty's Hessian
from \code{\link[=statmod_penalty_at]{statmod_penalty_at()}}. It is written once because three readers
want the same matrix at the same point and must not disagree about it --
\code{\link[=statmod_edf]{statmod_edf()}}'s smoother, \code{\link[=vcov.StatmodFit]{vcov.StatmodFit()}}'s variance and
\code{\link[=deficient_coords]{deficient_coords()}}'s rank test.

A kinked penalty contributes no curvature away from its kink, and any
non-finite entry would be the kink itself reached by a hair, so the
penalty passes through \code{\link[=zap_nonfinite]{zap_nonfinite()}} as it does everywhere else.
}
\seealso{
\code{\link[=statmod_information_at]{statmod_information_at()}}, \code{\link[=statmod_penalty_at]{statmod_penalty_at()}}
}
\keyword{internal}
