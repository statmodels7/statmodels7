% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{reject_nested_offsets}
\alias{reject_nested_offsets}
\title{Reject an Offset Buried Inside a Term}
\usage{
reject_nested_offsets(eq, param)
}
\arguments{
\item{eq}{A one-sided formula, with the equation's own offsets already
taken out by \code{\link[=split_offsets]{split_offsets()}}.}

\item{param}{The parameter the equation belongs to, named in the message.}
}
\value{
\code{NULL}, invisibly, when nothing is found. Signals an error naming
\code{param} and the offending call otherwise.
}
\description{
Stops where \code{offset()} appears anywhere other than as a term of the
equation itself.
}
\details{
\code{\link[=split_offsets]{split_offsets()}} takes the top-level additive terms, which is where R
recognizes an offset. One written inside another term's formula, as in
\code{ridge(~ z + offset(o))}, \code{random(~ 1 + offset(o) | g)} or
\code{nl(a ~ 0 + ridge(~ g + offset(o)))}, reaches that term's own
\code{model.matrix} and is dropped there exactly as it used to be dropped at
the equation level. Measured: the fit ran, the block carried the columns
of the model without it, and the intercept came back at 1.33 against the
-5.01 the offset gives, a factor of 566.

Such a term is \strong{refused} and not routed up to the equation, because the
meaning differs by where it sits. In a penalized term's formula an offset
would be a contribution to the equation's predictor, which is what writing
it at the equation level already says. In a subformula it would be a
contribution to that parameter's own chart, a different quantity. Picking
one reading would fit the wrong model in half the cases; the refusal names
the place it belongs and costs a caller one edit.

The whole expression tree is walked, so a term written later is covered.
}
\seealso{
\code{\link[=split_offsets]{split_offsets()}}
}
\keyword{internal}
