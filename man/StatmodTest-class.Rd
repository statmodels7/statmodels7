% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restricted.R
\name{StatmodTest-class}
\alias{StatmodTest-class}
\alias{StatmodTest}
\title{A Test of One Coefficient Against One Value}
\usage{
StatmodTest(
  test = character(0),
  statistic = integer(0),
  df = integer(0),
  p.value = integer(0),
  estimate = integer(0),
  null_value = integer(0),
  parameter = character(0),
  coefficient = character(0),
  mode_error = integer(0),
  converged = logical(0)
)
}
\arguments{
\item{test}{Which of the four statistics, as \code{\link[=statmod_test]{statmod_test()}} was asked
for it.}

\item{statistic}{Its value, compared with a \eqn{\chi^2_1}.}

\item{df}{One, always.}

\item{p.value}{The upper tail of that \eqn{\chi^2_1}.}

\item{estimate}{The unrestricted estimate of the coefficient.}

\item{null_value}{The value it was tested against.}

\item{parameter, coefficient}{Which coefficient of which equation.}

\item{mode_error}{How far above its own mode the restricted refit
stopped, in log-likelihood units; \code{NA} for the Wald statistic.}

\item{converged}{The inner optimizer's flag at the restricted refit;
\code{NA} for the Wald statistic.}
}
\value{
An object of class \code{StatmodTest}.
}
\description{
The result of testing that one coefficient of a fit equals one value,
carrying the statistic, its distribution, the p-value and what was
tested.
}
\details{
The four statistics are compared with the same \eqn{\chi^2_1}, so \code{df} is
always 1 and the object holds one kind of quantity whatever produced it.
}
\section{Whether the refit reached its mode}{
\code{mode_error} and \code{converged} both describe the restricted refit and both
are \code{NA} for the Wald statistic, which reads the unrestricted fit and
refits nothing. They answer different questions and only the first is
printed.

\code{converged} is the inner optimizer's flag: whether a stopping rule fired,
a boolean about a threshold on a score whose size depends on the model.
\code{mode_error} is \code{\link[=restricted_mode_error]{restricted_mode_error()}}, how much of the penalized
objective is still on the table at the point the refit returned, in
log-likelihood units against \code{\link[=mode_error_limit]{mode_error_limit()}} -- the rule statmodels7
0.81.0 established for exactly this question.

The flag is not merely a false negative: it is anti-correlated with the
quality of the point, because a stopping rule reads a change and a
distance reads a point. Measured on \code{y ~ x} with a Poisson response at
\eqn{n = 200} over nineteen held values, all nineteen are at their mode
and the four the flag rejects are the four best-located of them. See
\code{\link[=restricted_mode_error]{restricted_mode_error()}}.

A statistic read off a refit that stopped short is the value at wherever
it stopped, and \code{print()} says so -- reading \code{mode_error}, so that it says
it where it is true.

The class exists rather than a bare list because everything a caller of
this toolkit receives is an object with declared properties. R's own
tests return an \code{htest}, which is a list with a class attribute and no
contract; nothing here can validate one, and a reader cannot ask it what
it carries.
}

\examples{
dd <- data.frame(x = seq(-1, 1, length.out = 40))
dd$y <- rpois(40, exp(0.3 + dd$x))
fit <- statmod(y ~ x, distributions7::poisson_distrib(), dd)
S7::S7_inherits(statmod_test(fit, "mu", "x", 1), StatmodTest)

}
\seealso{
\code{\link[=statmod_test]{statmod_test()}}, which builds one, and
\code{\link[=restricted_mode_error]{restricted_mode_error()}}, which \code{mode_error} reports.
}
\keyword{internal}
