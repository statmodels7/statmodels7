% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path.R
\name{path_values}
\alias{path_values}
\title{The Values a Path Visits}
\usage{
path_values(pen, theta, name, s_max, n_values = 40L, min_ratio = 1e-04)
}
\arguments{
\item{pen}{A \pkg{penalties7} penalty.}

\item{theta}{The hyperparameters in force.}

\item{name}{Which one the path varies.}

\item{s_max}{The size of the kink at the top of the path.}

\item{n_values}{How many points.}

\item{min_ratio}{The smallest kink size, as a fraction of \code{s_max}.}
}
\value{
A numeric vector of values for \code{name}, from the emptiest fit to
the fullest.
}
\description{
A geometric grid of kink sizes from the one that empties the block down to
\code{min_ratio} of it, carried back onto the hyperparameter.
}
\details{
The grid is geometric in the size of the kink, never in the
hyperparameter, so that a penalty whose kink narrows as its hyperparameter
grows is swept in the same order as one whose kink widens: from the empty
model towards the full one. Values the penalty cannot reach are dropped.

The exponent relating the two is read ONCE and every target inverted
through it, in place of bracketing each target on its own. Measured, a
bracketing solve costs 4.18 ms against a fit's 62.5 ms, so a path of
twenty-five points spent 6.7 per cent of itself locating the values it
would visit; through the exponent the whole grid costs four evaluations of
the size. \code{\link[=kink_by_power]{kink_by_power()}} checks the relation before the values
are used and returns \code{NA} where it does not hold, and those fall back
to \code{\link[=kink_solve]{kink_solve()}} one at a time.
}
\seealso{
\code{\link[=kink_power]{kink_power()}}, \code{\link[=path_forced]{path_forced()}}
}
\keyword{internal}
