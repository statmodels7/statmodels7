% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iwls.R
\name{iwls_line_search}
\alias{iwls_line_search}
\title{One Scoring Step and Its Line Search}
\usage{
iwls_line_search(obj, beta, value, g, pc, method, damp, frozen, guard = FALSE)
}
\arguments{
\item{obj}{The objective.}

\item{beta}{The current coefficients.}

\item{value}{The objective there.}

\item{g}{The gradient there.}

\item{pc}{The pieces \code{\link[=iwls_solve]{iwls_solve()}} reads.}

\item{method}{The \code{\link[=Iwls]{Iwls()}} object.}

\item{damp}{The Levenberg damping.}

\item{frozen}{The held positions.}

\item{guard}{Whether an error at a trial point is read as a rejection.}
}
\value{
A list of \code{ok}, \code{cand}, \code{vnew}, \code{step_used}, \code{sol} and \code{errors},
the number of trial points whose objective raised.
}
\description{
Solves for the scoring increment from the given pieces and halves the step
until Armijo's condition holds or the budget of halvings is spent.
}
\details{
It is the body \code{\link[=iwls_fit]{iwls_fit()}} ran inline, moved into a function so that the
step can be tried twice at one iterate, first on the observed information
and then on the expected one, where \code{\link[=iwls_resolve]{iwls_resolve()}} settled the method
with the fallback. With \code{guard = FALSE} the operations are the inline ones,
in the same order. With \code{guard = TRUE} a trial point whose objective raises
an error is read as rejected rather than stopping the fit, and counted.
}
\seealso{
\code{\link[=iwls_fit]{iwls_fit()}}, its caller.
}
\keyword{internal}
