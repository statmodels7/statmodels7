% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{unfittable_reason}
\alias{unfittable_reason}
\title{Why a Term Is Outside the Fitting Scheme}
\usage{
unfittable_reason(term)
}
\arguments{
\item{term}{One built term.}
}
\value{
A single string.
}
\description{
Returns the reason a term cannot be assembled as a fixed design block, or
the empty string when it can.
}
\details{
Two reasons, and both are read from the term rather than from its class.

A covariance label (\code{\link[modelterms7:term_tag]{modelterms7::term_tag()}}) under a structural term of
the \strong{likelihood} shape says that a latent the likelihood integrates out
shares a prior with coefficients that are estimated. The two are integrated
by different routes and there is no one prior to share, so the term is
rejected rather than fitted as though the label were absent, which would be
a different model reported under the name of the one that was asked for. A
label under a \strong{filter} is fitted: its parameters are numbers estimated
beside the coefficients, and the block is read among them.

Whether a class is admissible is not a question about one term, and is
asked where every member is visible, by \code{\link[=class_space]{class_space()}}.

A structural term implementing neither shape of the contract is rejected
for the reason its message gives.
}
\seealso{
\code{\link[=reject_unfittable]{reject_unfittable()}}
}
\keyword{internal}
