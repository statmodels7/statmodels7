% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_gradient.R
\name{statmod_penalty_second}
\alias{statmod_penalty_second}
\title{How a Moving Penalty Hessian Enters the Criterion's Second Derivative}
\usage{
statmod_penalty_second(
  spec,
  design,
  coef,
  hyper,
  idx,
  m,
  l,
  bm,
  bl,
  bml,
  total
)
}
\arguments{
\item{spec}{A \code{\link[=StatmodSpec]{StatmodSpec()}}.}

\item{design}{The design.}

\item{coef}{The coefficients at the penalized mode.}

\item{hyper}{The hyperparameters.}

\item{idx}{The outer index.}

\item{m, l}{The two rows of the index.}

\item{bm, bl, bml}{The mode's movement along each and along the pair.}

\item{total}{The stacked width.}
}
\value{
A \code{total} square matrix, or \code{NULL} where no penalty's Hessian
moves with the coefficients.
}
\description{
The part of \eqn{\partial K_m/\partial t_l} that a penalty whose Hessian
depends on the coefficients contributes, placed in the stacked coefficient
space, where \eqn{K_m = S_m + T[b_m]} is the determinant's matrix
differentiated in hyperparameter \eqn{m}.
}
\details{
Differentiating \eqn{S_m(\beta, t) + \partial S/\partial\beta\,[b_m]} once
more along \eqn{t_l}, with \eqn{\beta} moving by \eqn{b_l}, gives
\deqn{\frac{\partial S_m}{\partial\beta}[b_l]
  + \frac{\partial S_l}{\partial\beta}[b_m]
  + \frac{\partial^2 S}{\partial\beta^2}[b_l, b_m]
  + \frac{\partial S}{\partial\beta}[b_{ml}]}
beside \eqn{S_{ml}}, which \code{\link[=outer_pieces]{outer_pieces()}} already carries. The first two
are one quantity by the symmetry of mixed partials, read from
\code{\link[penalties7:penalty_d2hessian_beta]{penalties7::penalty_dhessian_beta_theta()}} for the hyperparameters a
penalty owns; the third is \code{\link[penalties7:penalty_d2hessian_beta]{penalties7::penalty_d2hessian_beta()}} and the
fourth \code{\link[penalties7:penalty_dhessian_beta]{penalties7::penalty_dhessian_beta()}}. A penalty that
\code{\link[penalties7:beta_quadratic]{penalties7::beta_quadratic()}} calls quadratic contributes nothing, so a
model carrying only those gets \code{NULL} and its Hessian is untouched.

Measured on a Student t prior over 30 groups before it was written, the
assembly without these pieces is 122 per cent out and flat in the step, and
with them it converges on a central difference of the exact gradient as
\eqn{h^2} down to the reference's own floor.
}
\seealso{
\code{\link[=statmod_penalty_dbeta]{statmod_penalty_dbeta()}}, \code{\link[=statmod_marginal_hess]{statmod_marginal_hess()}},
\code{\link[=statmod_pe_derivs]{statmod_pe_derivs()}}
}
\keyword{internal}
