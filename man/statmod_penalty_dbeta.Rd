% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_gradient.R
\name{statmod_penalty_dbeta}
\alias{statmod_penalty_dbeta}
\alias{penalty_dbeta_trace}
\alias{penalty_dbeta_blocks}
\title{How a Penalty's Hessian Moves With the Mode}
\usage{
statmod_penalty_dbeta(spec, design, coef, hyper, v, total)

penalty_dbeta_trace(spec, design, coef, hyper, v, M)

penalty_dbeta_blocks(spec, design, coef, hyper, v)
}
\arguments{
\item{spec}{A \code{\link[=StatmodSpec]{StatmodSpec()}}.}

\item{design}{The design.}

\item{coef}{The coefficients at the penalized mode.}

\item{hyper}{The hyperparameters.}

\item{v}{The direction, over the stacked coefficients.}

\item{total}{The stacked width.}

\item{M}{The matrix the trace is taken against, \code{total} square.}
}
\value{
\code{statmod_penalty_dbeta()} a \code{total} square matrix, or \code{NULL} where
no penalty's Hessian moves; \code{penalty_dbeta_trace()} a single number;
\code{penalty_dbeta_blocks()} a list of \code{pos} and \code{T} pairs.
}
\description{
\eqn{\sum_u \partial S_u/\partial\beta\,[v]} over every penalty on the
stacked coefficients whose Hessian depends on them, placed in the stacked
coefficient space. \code{penalty_dbeta_trace()} is its trace against a matrix and
\code{penalty_dbeta_blocks()} the per-penalty pieces both are assembled from.
}
\details{
A marginal criterion's determinant is of \eqn{H + S}, so the mode's movement
\eqn{v} reaches it through \eqn{S} wherever \eqn{S} depends on the
coefficients, as it does for a heavy-tailed prior on a random effect, and the
piece is \eqn{\mathrm{tr}(M\,\partial S/\partial\beta[v])}. A
prediction-error criterion reads the matrix itself inside its trace. Every
such penalty contributes, not only the one owning the hyperparameter being
differentiated, because the mode moves in all of the coefficients at once.

A penalty that \code{\link[penalties7:beta_quadratic]{penalties7::beta_quadratic()}} calls quadratic is skipped,
which is every ridge, smooth and Gaussian random effect, so a model carrying
only those assembles nothing: the matrix is \code{NULL} and the trace exactly
\code{0}, which leaves its gradient identical to the bit. A kinked penalty is
skipped as well, and a penalty over a structural term's own parameters
belongs to the joint route.

Measured on a Student t prior over 30 groups, the gradient without this
piece is 4.3e-04 out and flat in the step; with it, 1.5e-06 against a
central difference of the criterion with the mode refitted.
}
\seealso{
\code{\link[=statmod_marginal_grad]{statmod_marginal_grad()}}, \code{\link[=statmod_pe_derivs]{statmod_pe_derivs()}},
\code{\link[penalties7:penalty_dhessian_beta]{penalties7::penalty_dhessian_beta()}}
}
\keyword{internal}
