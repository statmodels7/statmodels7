% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_gradient.R
\name{structural_blocks_data}
\alias{structural_blocks_data}
\title{The Same Pieces as Data for the Compiled Recursion}
\usage{
structural_blocks_data(params, ap, Vs, H, D3, n)
}
\arguments{
\item{params}{The distribution's parameter names.}

\item{ap}{Which of them carries the filter.}

\item{Vs}{The static rows.}

\item{H, D3}{The family's derivatives at the fitted predictors.}

\item{n}{The number of observations.}
}
\value{
A list with \code{H}, \code{D3}, \code{Vs} and \code{ap}.
}
\description{
The quantities the callback of \code{.structural_blocks()} reads, laid out
as matrices so \code{modelterms7}'s compiled second-order route can read
them without calling back into R: the mixed second derivatives one column
per distribution parameter (the filter's own column zero, the loop skips
it), the third derivatives one column per parameter pair with pair
\eqn{(r, r_2)} at column \eqn{(r-1)\,n_p + r_2}, the static jacobian rows
densified, and the filter's parameter index.
}
\keyword{internal}
