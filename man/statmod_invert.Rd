% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restricted.R
\name{statmod_invert}
\alias{statmod_invert}
\title{A Confidence Interval by Inverting a Test}
\usage{
statmod_invert(
  fit,
  param,
  coefname,
  level = 0.95,
  test = "lr",
  type = "bayesian",
  maxit = 40L
)
}
\arguments{
\item{fit}{A \code{\link[=StatmodFit]{StatmodFit()}}.}

\item{param, coefname}{Which coefficient, as in \code{\link[=statmod_restrict]{statmod_restrict()}}.}

\item{level}{The confidence level, a single number in (0, 1).}

\item{test}{One of \code{"lr"}, \code{"score"}, \code{"gradient"}, \code{"wald"}.}

\item{type}{Which variance sets the starting bracket, and which the Wald
statistic reads.}

\item{maxit}{How many widenings to try on each side before giving up.}
}
\value{
A numeric vector of two, \code{lower} and \code{upper}. An end the search
could not bracket is \code{NA}, which is what an unbounded interval looks
like and is reported rather than guessed at.
}
\description{
The set of values a test does not reject at \code{level}, found by locating the
two points where the statistic crosses its critical value.
}
\details{
An interval by inversion is \eqn{\{b : T(b) \le \chi^2_{1,\alpha}\}}, and
it is not symmetric about the estimate unless the statistic is a parabola
in \eqn{b} -- which the Wald statistic is BY CONSTRUCTION, the curvature
being read once at \eqn{\hat\beta}, and which the other three are not.
That asymmetry is the whole reason to invert one of the others: it follows
the likelihood's own shape rather than a quadratic fitted at its top.

The search starts from the Wald interval, widens by a factor of 1.6 until
the statistic exceeds its critical value, and then bisects. Each
evaluation is one restricted refit, warm-started at the unrestricted
estimates and run at the fitted hyperparameters, so an interval costs a
few dozen inner fits and no outer search at all.
}
\seealso{
\code{\link[=statmod_stat]{statmod_stat()}}, the statistic being inverted.
}
\keyword{internal}
