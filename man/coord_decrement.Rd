% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{coord_decrement}
\alias{coord_decrement}
\title{What One Coordinate Alone Would Buy}
\usage{
coord_decrement(g, A)
}
\arguments{
\item{g}{The outer criterion's gradient.}

\item{A}{The negated symmetric curvature.}
}
\value{
A numeric vector as long as \code{g}.
}
\description{
\eqn{g_j^2/(2A_{jj})}, the decrement of the one-coordinate problem.
}
\details{
\code{\link[=statmod_certificate]{statmod_certificate()}} reads it for the boundary label, where a verdict
over the whole vector would say nothing about which coordinate has stopped
moving. It is the same maximum \code{\link[=joint_decrement]{joint_decrement()}} takes, restricted to
one coordinate, so it is bounded by the joint reading and a coordinate it
calls settled had already passed the test the verdict applies -- which is
what keeps \code{edge} a label and never a verdict.

As a verdict of its own it is the weaker reading, and that is why it is not
one: over the same 1350 fits it misses 10 of the 552 that the joint
decrement finds.

\code{Inf} where the diagonal is not positive, so that such a coordinate is
never called settled.
}
\seealso{
\code{\link[=joint_decrement]{joint_decrement()}}, \code{\link[=statmod_certificate]{statmod_certificate()}}
}
\keyword{internal}
