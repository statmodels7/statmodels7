% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outer_refresh.R
\name{kink_positions}
\alias{kink_positions}
\title{Where the Objective Has a Kink, in the Stacked Coefficients}
\usage{
kink_positions(spec, design, coef, split)
}
\arguments{
\item{spec}{A \code{\link[=StatmodSpec]{StatmodSpec()}}.}

\item{design}{The design.}

\item{coef}{The coefficients, a named list with one numeric vector per
parameter.}

\item{split}{The stacked positions of each parameter's coefficients, a
named list as the objective's \code{split} returns it for \code{seq_along()} of the
stacked vector.}
}
\value{
An integer vector of stacked positions, possibly empty.
}
\description{
Asks every term that recomputes its own block where, among its own
coefficients, the objective is not differentiable at the coefficients
given, through \code{\link[modelterms7:term_kinks]{modelterms7::term_kinks()}}, and carries the answer into
the positions of the stacked vector. \code{\link[=iwls_fit]{iwls_fit()}} holds those positions
where a line search rejects every step length.
}
\seealso{
\code{\link[=iwls_fit]{iwls_fit()}}, \code{\link[=refresh_units]{refresh_units()}}.
}
\keyword{internal}
