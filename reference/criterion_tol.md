# The Tolerance a Criterion Asks For

The largest `tol` in a criterion, walking a combined one into its parts.

## Usage

``` r
criterion_tol(crit)
```

## Arguments

- crit:

  An optimizers7 criterion.

## Value

A single number.
