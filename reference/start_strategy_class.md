# The start_strategy Class Object

Fetched rather than captured, so that a test of membership cannot be
fooled by the class being re-created — which is what covr does.

## Usage

``` r
start_strategy_class()
```

## Value

The
[`start_strategy`](https://statmodels7.github.io/statmodels7/reference/start_strategy.md)
class object.
