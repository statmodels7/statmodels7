# The Message for an Unrecognized Prediction Target

The Message for an Unrecognized Prediction Target

## Usage

``` r
unknown_what(what, params)
```

## Arguments

- what:

  What was asked for.

- params:

  The family's parameter names.

## Value

A single string.
