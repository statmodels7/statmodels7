# The Block a Path Row Belongs To

The Block a Path Row Belongs To

## Usage

``` r
path_block(blocks, row)
```

## Arguments

- blocks:

  The blocks.

- row:

  One row of
  [`path_rows`](https://statmodels7.github.io/statmodels7/reference/path_rows.md)'s
  index.

## Value

One entry of `blocks$sparse`.
