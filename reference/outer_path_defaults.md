# The Defaults a Path Carries

The properties every
[`OuterMethod`](https://statmodels7.github.io/statmodels7/reference/OuterMethod-class.md)
needs whether or not it uses them, so that one class serves every
criterion.

## Usage

``` r
outer_path_defaults(over = NULL)
```

## Value

A named list.
