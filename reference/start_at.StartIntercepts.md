# Starting Values From the Intercept-Only Fit

The default strategy's answer.

## Arguments

- strategy:

  A `StartIntercepts` object.

- spec, design, obj, ...:

  As in
  [`start_at()`](https://statmodels7.github.io/statmodels7/reference/start_at.md).

## Value

A named list of numeric vectors.
